# Staging dir assembled by build.sh for the backend image context.
.build-context/
# Artifacts from the retired host-build scheme; build.sh no longer writes these.
bin/
web/
# Local overrides / secrets.
.env
