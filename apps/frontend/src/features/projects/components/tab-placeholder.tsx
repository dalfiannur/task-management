/** Placeholder for a project tab whose feature flow isn't built yet. */
export function TabPlaceholder({ title }: { title: string }) {
  return (
    <div className="p-8 text-sm text-muted-foreground">
      {title} — coming in its own flow.
    </div>
  );
}
