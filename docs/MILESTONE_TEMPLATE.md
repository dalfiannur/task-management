# Milestone `<N>` — `<Nama Milestone>`

> Salin file ini menjadi `MILESTONE_<N>.md`. Satu milestone = satu potongan ruang lingkup dengan kriteria selesai yang tak ambigu.

## Tujuan

`<Satu paragraf: apa yang milestone ini wujudkan dan mengapa sekarang.>`

## Ruang lingkup

**Termasuk:**

- `<hal yang dikerjakan.>`
- `<...>`

**Tidak termasuk (sengaja ditunda):**

- `<hal yang secara eksplisit di luar milestone ini, agar tak ada asumsi.>`
- `<...>`

## Artefak yang dihasilkan

| Artefak | Bentuk |
| --- | --- |
| `<mis. RFC-00xx>` | Proposal `<...>` |
| `<mis. ADR-00xx>` | Keputusan `<...>` |
| `<mis. schema/v1/...>` | Kontrak `<...>` + contoh valid |
| `<mis. kode + tes>` | Implementasi `<...>` |

## Kriteria selesai (Definition of Done)

Milestone dianggap **selesai** ketika semua benar:

- [ ] `<kriteria yang dapat diperiksa 1.>`
- [ ] `<kriteria 2 — mis. semua kontrak baru punya schema + contoh valid.>`
- [ ] `<kriteria 3 — mis. validator/tes hijau di CI.>`
- [ ] `<kriteria 4 — mis. setiap keputusan konsekuensial punya ADR.>`

## Ketergantungan

- **Butuh selesai lebih dulu:** `<milestone/artefak prasyarat.>`
- **Membuka jalan bagi:** `<milestone berikutnya.>`

## Pertanyaan terbuka

- `<pertanyaan yang belum terjawab → catat sebagai RN bila perlu.>`
